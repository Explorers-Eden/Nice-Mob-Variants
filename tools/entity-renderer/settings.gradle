pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' }
        mavenCentral()
        gradlePluginPortal()
    }
}

// Keep project repositories allowed. Fabric Loom adds internal project repositories while applying.
rootProject.name = 'entity-renderer'
